# Logs
logs/*.json
logs/*.jsonl
logs/*.log

# Call-history database (engine dev/office runs)
data/
*.db

# The Console's shared password (the example file is the committed one)
config/console.json

# Frontend toolchain. web/dist IS committed — the booth host has no toolchain.
web/node_modules/

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
