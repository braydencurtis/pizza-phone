# Configuration

Runtime configuration files that control behavior without code changes.

## Key Files

- `pizza-phone.json` — Active mode, daily code, attempt limits, and feature flags
- `prompts/` — Prompt library metadata (file paths, node definitions for Roguelike tree)
